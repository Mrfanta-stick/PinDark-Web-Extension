{
  "name": "pindark",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for PinDark — packs the extension source into a release ZIP for the Chrome Web Store and Opera Add-ons.",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
